package handlers

// Note: extractUserID is defined in moderation_handler.go and is shared across all handlers.
// This file exists to document that the function is package-level and reusable.
// If moderation_handler.go is ever removed, move extractUserID here.
